.page.page--sections
  .page-section
    .container
      // If no search results
      -#.search-row
        .search-row__inner
          %form.search-row__form(action='#')
            %input(type='text' value='абв')
            %button(type='submit')
              %svg.icon(role='img' data-size='34')
                %use(xlink:href='/assets/courses_shop/icons.svg#search')

          %p.search-row__results По&nbsp;вашему запросу ничего не&nbsp;найдено (
          .h3.search-row__related Возможно вас заинтересует:

      .search-row
        .search-row__inner
          %form.search-row__form(action='#')
            %input(type='text' value='косметология')
            %button(type='submit')
              %svg.icon(role='img' data-size='34')
                %use(xlink:href='/assets/courses_shop/icons.svg#search')

          %p.search-row__results По&nbsp;вашему запросу найдено <b>16&nbsp;курсов</b>:

      = render 'layouts/courses_shop/components/courses_items'

      = render 'layouts/courses_shop/components/pagination_complex'